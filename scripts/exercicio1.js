/* declara as variáveis usadas na soma */
let INDICE = 13, SOMA = 0, K = 0; 

/* laço de repetição, repete enquanto a condição for verdadeira */
while (K < INDICE) {
    K++; /* a cada repetição a variavel K soma 1 */
    SOMA = SOMA + K
}
console.log(SOMA);